# Session Summary - 2026-03-05 (Part 2)

## Fix: Missing resource_user_associations for auto-created resources

### Problem
Employee "dtroeva" (Dimana Troeva) had her `first_name`/`last_name` correctly set in the `resources` table, but the name wasn't displaying on the Projects, Employees, or Burn pages. Instead, the raw Clockify system ID "dtroeva" was shown.

### Root Cause
The `auto_create_resource_on_timesheet_insert()` trigger (migration 007) creates a `resources` record when a new user appears in timesheet data, but does NOT create a corresponding `resource_user_associations` record. Migration 014 backfilled associations for all existing resources at that time, but any resource auto-created after migration 014 was missing its association.

Without the association, `userIdToDisplayNameLookup` (used by all three pages) cannot map the Clockify `user_id` to the resource's `first_name`/`last_name`, so it falls back to `entry.user_name` from the raw timesheet data.

### Fix (Migration 060)
1. **Updated trigger** — `auto_create_resource_on_timesheet_insert()` now also inserts into `resource_user_associations` when creating a new resource, preventing this gap for future employees
2. **Backfilled missing associations** — finds any existing resources with a `user_id` that lack a corresponding association record and creates them

### Name Resolution Pipeline (Reference)
```
Timesheet entry (user_id)
  -> resource_user_associations (user_id -> resource_id)
    -> v_entity_canonical (resource_id -> canonical_resource_id)
      -> resources (canonical_resource_id -> first_name + last_name)
        -> Display name on page

Fallback: entry.user_name (raw Clockify name)
```

### Additional Finding: "Unassigned" Company Not Grouped
Dimana's entries showed under "Unassigned" instead of "The B Team" on the Employees page. Investigation revealed:
- The B Team's company group had "Unknown Client" as a member, not "Unassigned"
- Dimana's timesheet entries have `client_id = '__UNASSIGNED__'` mapping to the "Unassigned" company
- These are two different company records — "Unassigned" was never added to The B Team's group
- Resolution: Add "Unassigned" to The B Team's company group via Company Management page

---

## Security Hardening (Migrations 061-064)

### Overview
Addressed 5 security vulnerabilities found during an audit. All fixes are backward-compatible with zero impact on legitimate admin usage.

### 1. execute_readonly_sql RLS Bypass (Migration 061)
- **Issue**: SECURITY DEFINER function granted to `authenticated` role bypassed all RLS, letting any user read all tables via arbitrary SELECT
- **Fix**: `REVOKE EXECUTE ON FUNCTION execute_readonly_sql(TEXT) FROM authenticated` — only `service_role` (Edge Function) retains access

### 2. JWT Signature Not Verified in customer-revenue-report (Code Fix)
- **Issue**: Edge Function decoded JWT payload with `atob()` without verifying signature — forged tokens with `role=service_role` would pass
- **Fix**: Replaced with `jwtVerify()` from `jose` library using `SUPABASE_JWT_SECRET`

### 3. Chat Edge Function Open to All Users (Code Fix)
- **Issue**: Any authenticated user could use the AI chat, which creates a `service_role` client bypassing all RLS to execute SQL
- **Fix**: Added `is_admin()` RPC check after user verification, matching the `admin-users` Edge Function pattern

### 4. Financial Tables Writable by All Users (Migration 062)
- **Issue**: `projects`, `project_monthly_billing_limits`, `project_monthly_active_status`, and `billing_month_status` allowed UPDATE/INSERT from any authenticated user
- **Fix**: All write policies now require `is_admin()`. SELECT remains open for all authenticated users.

### 5. Core Tables Missing RLS Entirely (Migration 063)
- **Issue**: `resources` and `timesheet_daily_rollups` never had RLS enabled — any authenticated user could read and write all rows
- **Fix**: Enabled RLS on both. `resources`: read=all, write=admin. `timesheet_daily_rollups`: read=all, write=service_role only.

### 6. admin_users_view Exposed to All Users (Migration 064)
- **Issue**: View exposing all user emails, roles, sign-in times was queryable by any authenticated user
- **Fix**: `REVOKE SELECT ON admin_users_view FROM authenticated` — only SECURITY DEFINER RPCs consume it

### Files Changed

| File | Change |
|------|--------|
| `supabase/migrations/060_fix_auto_create_resource_association.sql` | Fix trigger + backfill missing associations |
| `supabase/migrations/061_revoke_readonly_sql_from_authenticated.sql` | Revoke RLS-bypassing function from authenticated |
| `supabase/migrations/062_restrict_financial_tables_to_admin.sql` | Admin-only write policies on 4 financial tables |
| `supabase/migrations/063_enable_rls_resources_and_timesheets.sql` | Enable RLS on resources + timesheet_daily_rollups |
| `supabase/migrations/064_revoke_admin_users_view_from_authenticated.sql` | Revoke admin_users_view from authenticated |
| `supabase/functions/customer-revenue-report/index.ts` | JWT signature verification with jose |
| `supabase/functions/chat/index.ts` | Admin-only access check |

### Commits
1. `e075401` - fix: Auto-create resource_user_associations in timesheet trigger
2. `b4c025c` - security: Revoke execute_readonly_sql from authenticated role
3. `02be20e` - security: Verify JWT signature in customer-revenue-report Edge Function
4. `dfe0947` - security: Restrict chat Edge Function to admin users only
5. `e7f4883` - security: Restrict financial table writes to admin users only
6. `0eb2d80` - security: Enable RLS on resources and timesheet_daily_rollups
7. `438ccb3` - security: Revoke admin_users_view from authenticated role

### Deployment Notes
- Migrations 060-064 must be run in the Supabase SQL editor
- Edge Functions require redeployment: `supabase functions deploy chat` and `supabase functions deploy customer-revenue-report`
